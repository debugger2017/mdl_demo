<%= form_for(@comment , url: comments_path) do |f| %>
  <%= render 'shared/error_messages_users' %>
  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
