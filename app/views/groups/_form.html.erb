<%= form_for(@group , url: groups_path) do |f| %>
  <%= render 'shared/error_messages_groups' %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
