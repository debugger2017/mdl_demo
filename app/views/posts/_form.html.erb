<%= form_for(@post , url: posts_path) do |f| %>
  <%= render 'shared/error_messages_posts' %>
  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
